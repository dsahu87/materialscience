# Define values with a specific interval from the first to the last number
# Need values from 15 to 90 with the interval of 15 values
x<-seq(30,180,30)
#Paste the values with the comma ","
paste(x,collapse=", ")
# Paste the values with the differantiating by the quotation " "
paste(x,sep=",")
